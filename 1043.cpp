#include <iostream>

using namespace std;

int main() 
{
    int n,m;
    cin >> n >> m;
    int chk[52];

    int tmp1;
    cin >> tmp1;
    while(tmp1--) {
        int tmp2;
        cin >> tmp2;
        chk[tmp2] = 1;
    }

    for (int i=1; i<=m; i++) {
        int cnt;
        cin >> cnt;
        while(cnt--) {
            int tmp;
            cin >> tmp;
            
        }
    }
}