#include <stdio.h>
#include <vector>

using namespace std;

vector <int> ans;

int cnt = 0;
int A[100002];
int ans[0];

int main() 
{
    int n;
    scanf("%d",&n);

    for (int i=1; i<=n; i++) scanf("%d",&A[i]);
    for (int i=n; i>=1; i--) {

    }
}