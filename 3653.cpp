#include <iostream>
#include <cmath>

using namespace std;

int main() 
{
    int n,m;
    cin >> n >> m;

    int lcnt = 0;
    while(pow(2,lcnt) <= n) lcnt++;

    while(m--) {
        int tmp;
        cin >> tmp;
        
    }
}